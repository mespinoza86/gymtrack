import{Router}from'express';import{z}from'zod';import*as c from'../controllers/messages.controller.js';import{requireAuth}from'../middleware/auth.js';import{validate}from'../middleware/validate.js';import{asyncHandler}from'../utils/async-handler.js';const r=Router();r.use(requireAuth);r.get('/',asyncHandler(c.conversations));r.get('/:id',asyncHandler(c.messages));r.post('/:id',validate(z.object({body:z.string().trim().min(1).max(4000)})),asyncHandler(c.send));export default r;
