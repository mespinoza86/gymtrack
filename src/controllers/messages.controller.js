import*as s from'../services/messages.service.js';export async function conversations(req,res){res.json({conversations:await s.conversations(req.session.user)});}export async function messages(req,res){res.json({messages:await s.messages(req.params.id,req.session.user.id)});}export async function send(req,res){const message=await s.send(req.params.id,req.session.user.id,req.body.body);req.app.get('io').to(`conversation:${req.params.id}`).emit('message:new',message);res.status(201).json({message});}
